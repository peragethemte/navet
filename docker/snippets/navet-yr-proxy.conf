# Same-origin met.no relay for production Docker nginx. njs authenticates callers and injects
# the required User-Agent header and server-configured coordinates before fetching; unlike the
# RSS proxy there is no user-supplied URL, so no DNS-pinning transport hop is needed.
location /__navet_yr_proxy__/ {
  js_content navet_yr_proxy.handle;
}
